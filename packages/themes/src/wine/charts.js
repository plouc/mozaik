import colors from './colors'

export default {
    axis: {
        textColor: colors.text,
        tickColor: '#9b2e2e',
        legendColor: colors.text,
    },
    grid: {
        stroke:          '#9b2e2e',
        strokeDasharray: '3,6',
    },
}
