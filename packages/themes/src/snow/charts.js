import colors from './colors'

export default {
    axis: {
        textColor: colors.text,
        tickColor: '#bbb',
        legendColor: colors.text,
    },
    grid: {
        stroke: '#bbb',
    },
}
