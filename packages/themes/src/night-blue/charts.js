import colors from './colors'

export default {
    axis: {
        textColor: colors.text,
        tickColor: '#161f2d',
        legendColor: colors.text,
    },
    grid: {
        stroke: '#161f2d',
    },
}
